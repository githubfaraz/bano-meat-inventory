services:
  - type: web
    name: bano-fresh-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: MONGO_URL
        value: mongodb+srv://bano_admin:j7vT6fGRnpMcJrpS@bano-fresh.armyfua.mongodb.net/?retryWrites=true&w=majority&appName=bano-fresh
      - key: DB_NAME
        value: bano_fresh
      - key: JWT_SECRET_KEY
        value: bano-fresh-secret-key-2025-production-secure
      - key: CORS_ORIGINS
        value: "*"
