# MongoDB Configuration
MONGO_URL="mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority&appName=your-app"
DB_NAME="bano_fresh"

# Security Configuration
JWT_SECRET_KEY="change-this-to-a-secure-random-string-in-production"

# CORS Configuration (adjust based on your frontend domain)
# For production VPS, set this to your actual frontend domain
# Example: CORS_ORIGINS="https://banofresh.com,https://www.banofresh.com"
CORS_ORIGINS="*"

# Instructions:
# 1. Copy this file to .env
# 2. Update MONGO_URL with your MongoDB Atlas connection string
# 3. Generate a secure JWT_SECRET_KEY (use: openssl rand -hex 32)
# 4. Update CORS_ORIGINS with your actual frontend domain(s)
